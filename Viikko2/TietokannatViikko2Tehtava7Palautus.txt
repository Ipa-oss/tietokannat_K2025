#Tehtava7a
select * from employees order by last_name limit 10;

#Tehtava7b
select * from employees order by last_name, first_name limit 10;

#Tehtava7c
select * from employees order by hire_date desc limit 5;

#Tehtava7d
select first_name, last_name, salary from employees join salaries on employees.emp_no = salaries.emp_no where to_date='9999-01-01' order by salary desc limit 1;

#Tehtava7e
select first_name, last_name, salary from employees join salaries on employees.emp_no = salaries.emp_no where to_date='9999-01-01' order by salary limit 1;

#Tehtava7f
select first_name, last_name, salary from employees join salaries on employees.emp_no = salaries.emp_no where salary>='150000' and to_date='9999-01-01';

#Tehtava7g
select dept_name,count(*) from departments join dept_emp on departments.dept_no = dept_emp.dept_no join employees on dept_emp.emp_no = employees.emp_no where to_date='9999-01-01' and (dept_name='Sales' or dept_name='Marketing') group by dept_name;

#Tehtava7h
select first_name,last_name,dept_name from departments join dept_manager on departments.dept_no = dept_manager.dept_no join employees on dept_manager.emp_no = employees.emp_no where to_date='9999-01-01';

#Tehtava7i
select dept_name,avg(salary) from salaries join dept_emp on salaries.emp_no = dept_emp.emp_no join departments on dept_emp.dept_no = departments.dept_no where salaries.to_date='9999-01-01' and dept_emp.to_date='9999-01-01' and (dept_name='Marketing' or dept_name='Sales') group by dept_name;
